# - Config file for the LIBBITCOIN_EXPLORER package
#
#  LIBBITCOIN_EXPLORER_INCLUDE_DIRS - include directories for LIBBITCOIN_EXPLORER
#  LIBBITCOIN_EXPLORER_LIBRARIES    - libraries to link against

get_filename_component(LIBBITCOIN_EXPLORER_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(LIBBITCOIN_EXPLORER_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

include("${LIBBITCOIN_EXPLORER_CMAKE_DIR}/libbitcoin-explorerTargets.cmake")
set(LIBBITCOIN_EXPLORER_LIBRARIES libbitcoin-explorer)

find_package(libbitcoin-client 3.0.0 REQUIRED)
find_package(libbitcoin-network 3.0.0 REQUIRED)
